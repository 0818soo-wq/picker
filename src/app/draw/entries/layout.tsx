import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "[접수목록] '26.하 CSM전략회의",
};

export default function EntriesLayout({ children }: { children: React.ReactNode }) {
  return children;
}
