import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "[참여자현황] '26.하 CSM전략회의",
};

export default function StatusLayout({ children }: { children: React.ReactNode }) {
  return children;
}
