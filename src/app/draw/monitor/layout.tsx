import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "[관리메인] '26.하 CSM전략회의",
};

export default function MonitorLayout({ children }: { children: React.ReactNode }) {
  return children;
}
