import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "[로그인] '26.하 CSM전략회의",
};

export default function AdminLoginLayout({ children }: { children: React.ReactNode }) {
  return children;
}
